using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class GameManager : MonoBehaviour
{
    [SerializeField] public SlimeSet slime;
    [SerializeField] public Attacker slimeAttackerPrefab;
    [SerializeField] public Attack slimeAttackPrefab;
    [SerializeField] public float slimeMoveSpeed;
    [SerializeField] public float slimeHealth;
    [SerializeField] public float slimeAttackSpeed;
    [SerializeField] public float slimeDamage;
    [SerializeField] public float slimeSpeed;
    [SerializeField] public float slimeRange;

    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
